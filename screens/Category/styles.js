import { StyleSheet } from 'react-native';
import { categoryCard } from '../../styles/categoryCard';

const styles = StyleSheet.create({
  categoriesItemContainer: categoryCard.categoriesItemContainer,
  categoriesName: categoryCard.categoriesName,
  categoriesPhoto: categoryCard.categoriesPhoto,
  categoriesInfo: categoryCard.categoriesInfo
})

export default styles;